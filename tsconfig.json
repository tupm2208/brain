{
  // Solution file at the root: `tsc --build` with no arguments reads this.
  // Without it `npm run build` fails with TS5083 on a freshly cloned machine.
  "files": [],
  "references": [
    { "path": "packages/contract" },
    { "path": "packages/brain" },
    { "path": "packages/xeon" }
  ]
}
